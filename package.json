{
  "name": "audio-library",
  "version": "0.1.0",
  "description": "Interaktivní hudební knihovna — TypeScript + OOP. Ročníkový projekt, IT2A.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
